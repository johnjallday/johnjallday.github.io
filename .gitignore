# macOS
.DS_Store

# Editors / tooling
.vscode/
.idea/
.claude/
*.swp

# Node (in case tooling is added later)
node_modules/
package-lock.json

# Desktop-licensed font files — must never be deployed publicly.
# Web-licensed .woff2 files in fonts/ ARE tracked (those are legal to host).
*.otf
*.ttf

# Local environment / credentials
.env
.env.*
